//import 'generateRandomNumberInRange' and execute

import generateRandomNumberInRange from "generateRandomNumberInRange.js";

const limit = 10;
const randomNum = generateRandomNumberInRange(limit);

console.log(`Random number within the range of ${limit}: ${randomNum}`);
